<Character>:
    size_hint: None, None
    size: [self.radius * self.scale * 2. for wh in 'wh']

    canvas.before:
        Color:
        Rectangle:
            pos: self.pos
            size: self.size
            texture: self.atlas['%s_%02d' % (self.last_action, self.animation_frame)]
